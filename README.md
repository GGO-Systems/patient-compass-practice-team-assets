# Patient Compass Practice Team Campaign Assets

Public static assets for the Patient Compass practice-team awareness campaign.

These files are used as public media URLs for scheduled Buffer posts.

The `ui/` folder contains real Product UI screenshots used as references and inserts for HeyGen video generation.

The `marketing-screenshots/` folder mirrors the broader marketing screenshot library for screen-led videos and campaign review.
